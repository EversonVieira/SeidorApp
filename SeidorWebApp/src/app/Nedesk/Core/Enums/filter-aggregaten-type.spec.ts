import { FilterAggregatenType } from './filter-aggregaten-type';

describe('FilterAggregatenType', () => {
  it('should create an instance', () => {
    expect(new FilterAggregatenType()).toBeTruthy();
  });
});
